kind: pipeline
type: docker
name: triggered-drone

trigger:
  event:
    include:
    - push
    - pull_request

concurrency:
  limit: 1

steps:
- name: check branch
  image: node
  commands:
  - echo $DRONE_REPO
- name: test
  image: node
  commands:
  - npm install
  - npm test
