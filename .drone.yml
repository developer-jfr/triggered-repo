kind: pipeline
type: docker
name: triggered-drone

trigger:
  event:
    - pull_request
    - manual

concurrency:
  limit: 1

steps:
- name: check branch
  image: node
  commands:
  - echo $DRONE_REPO_BRANCH
- name: test
  image: node
  commands:
  - npm install
  - npm test
