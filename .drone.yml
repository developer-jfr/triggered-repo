kind: pipeline
type: docker
name: triggered-drone

trigger:
  event:
    include:
    - pull_request
    - custom

steps:
- name: env
  image: frdevelopergen/check-branch:2
  commands:
  - env
- name: test
  image: node
  commands:
  - npm install
  - npm test
