kind: pipeline
type: docker
name: triggered-drone

trigger:
  event:
    include:
    - pull_request
    - custom

steps:
- name: env
  image: frdevelopergen/check-branch:3
  commands:
  - ls
- name: test
  image: node
  commands:
  - npm install
  - npm test
