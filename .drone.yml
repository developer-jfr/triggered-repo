kind: pipeline
type: docker
name: triggered-drone

trigger:
  event:
    include:
    - push
    - pull_request

concurrency:
  limit: 1

steps:
- name: check branch
  image: badouralix/curl-jq
  commands:
  - |
    echo "Hello World"
- name: test
  image: node
  commands:
  - npm install
  - npm test
